<div id="renameBackupModal" class="hidden fixed inset-0 bg-black/40 dark:bg-black/60 backdrop-blur-sm z-50 flex items-center justify-center p-4">
  <div class="bg-white dark:bg-neutral-900 border border-neutral-200 dark:border-neutral-800 rounded-xl w-full max-w-sm shadow-2xl">
    <div class="flex items-start justify-between px-5 py-4 border-b border-neutral-200 dark:border-neutral-800">
      <div>
        <h2 class="text-sm font-semibold text-neutral-900 dark:text-white">Rename backup</h2>
        <p class="text-xs text-neutral-500 mt-0.5">Only changes the display name</p>
      </div>
      <button onclick="hideModal('renameBackupModal')" class="text-neutral-400 hover:text-neutral-700 dark:hover:text-white transition-colors">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>
    <div class="p-5">
      <label class="block text-xs font-medium text-neutral-600 dark:text-neutral-400 mb-1.5">New name <span class="text-red-500">*</span></label>
      <input type="text" id="renameInput" maxlength="100" placeholder="Backup name"
        class="w-full px-3 py-2.5 bg-neutral-50 dark:bg-neutral-800 border border-neutral-200 dark:border-neutral-700 rounded-lg text-sm text-neutral-900 dark:text-white placeholder-neutral-400 dark:placeholder-neutral-600 focus:outline-none focus:border-neutral-400 dark:focus:border-neutral-600 transition-colors">
    </div>
    <div class="flex gap-2.5 px-5 pb-5">
      <button onclick="hideModal('renameBackupModal')"
        class="flex-1 py-2.5 text-sm text-neutral-600 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-white bg-neutral-100 dark:bg-neutral-800 border border-neutral-200 dark:border-neutral-700 rounded-lg transition-all">
        Cancel
      </button>
      <button onclick="handleRenameBackup()"
        class="flex-1 py-2.5 text-sm text-white bg-neutral-800 dark:bg-neutral-700 hover:bg-neutral-700 dark:hover:bg-neutral-600 rounded-lg transition-colors font-medium">
        Save
      </button>
    </div>
  </div>
</div>

<script>
async function handleRenameBackup() {
  const name = document.getElementById('renameInput').value.trim();
  if (!name) { showToast('Name is required', 'error'); return; }
  hideModal('renameBackupModal');
  try {
    const { success, error } = await fetch(`/parachute/api/backup/${currentBackupId}/rename`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name }),
    }).then(r => r.json());
    if (success) { showToast('Backup renamed', 'success'); loadBackups(); }
    else showToast(error || 'Failed to rename', 'error');
  } catch { showToast('Network error', 'error'); }
}
</script>
